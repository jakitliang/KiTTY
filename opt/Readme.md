## Opt

This folder is for optional user packages and will not be automatically injected into the PATH.

Use `%lib_path% enhance_path "%kitty_root%\[path to folder]"` in `%kitty_root%\config\user_profile.cmd` or `%kitty_root%\config\profile.d\*.cmd` to add to the path.
